- **<Task step heading>** <multiline task step description with inline `code{:lang}` emphasizing, e.g. `input(){:ts}`>

---

- **<Next Task step heading>** <multiline task step description with inline `code{:lang}` emphasizing, e.g. `[content]{:html}`>

---

- **<Repeat n Task step heading until whole task is described>** <multiline task step description with inline `code{:lang}` emphasizing>
